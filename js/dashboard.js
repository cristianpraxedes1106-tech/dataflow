document.addEventListener(
    "DOMContentLoaded",
    () => {

        createRevenueChart();

        createCategoryChart();

        createComparisonChart();

        renderInsights();

    }
);